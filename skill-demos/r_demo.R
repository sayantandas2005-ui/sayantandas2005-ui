# Small statistical analysis demonstration in R

values <- c(12, 15, 18, 20, 25, 27, 30)

cat("Mean:", mean(values), "\n")
cat("Median:", median(values), "\n")
cat("Standard deviation:", sd(values), "\n")

summary(values)
